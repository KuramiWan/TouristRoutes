<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.user.contactPerson.dao.ContactPersonDao">

    <resultMap type="org.jeecg.modules.user.contactPerson.entity.ContactPerson" id="ContactPersonMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="realName" column="real_name" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ContactPersonMap">
        select id,
               real_name,
               phone
        from contact_person
        where = #{}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ContactPersonMap">
        select
        id, real_name, phone
        from contact_person
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="realName != null and realName != ''">
                and real_name = #{realName}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from contact_person
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="realName != null and realName != ''">
                and real_name = #{realName}
            </if>
            <if test="phone != null and phone != ''">
                and phone = #{phone}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into contact_person(id, real_name, phone)
        values (#{id}, #{realName}, #{phone})
    </insert>

    <insert id="insertBatch" keyProperty="" useGeneratedKeys="true">
        insert into contact_person(id, real_name, phone)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.realName}, #{entity.phone})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="" useGeneratedKeys="true">
        insert into contact_person(id, real_name, phone)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.realName}, #{entity.phone})
        </foreach>
        on duplicate key update
        id = values(id),
        real_name = values(real_name),
        phone = values(phone)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update contact_person
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="realName != null and realName != ''">
                real_name = #{realName},
            </if>
            <if test="phone != null and phone != ''">
                phone = #{phone},
            </if>
        </set>
        where = #{}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from contact_person
        where = #{}
    </delete>

</mapper>

